/** @type {import('next').NextConfig} */
const nextConfig = {
   
    // plugins:[
    //     webpack.IgnorePlugin({
    //         resourceRegExp:/^fs$|^net$/
    //     })
    // ]
}

module.exports = nextConfig
